import { Button } from "components";

export function MyAccountButton() {
  return (
    <Button
      variant="letter"
      icon={{ name: "outlineUser" }}
    >
      Minha conta
    </Button>
  );
}
