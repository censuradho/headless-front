import { styled } from "stitches.config";

export const Button = styled('button', {
  display: 'flex',
  alignItems: 'center',
  gap: '0.5rem',
  
  variant: {
    letter: {
      background: 'none',
    }
  }
})