import { styled } from "stitches.config";

export const Container = styled("div", {
  padding: "1rem",
  marginBottom: "1rem",
});

export const Form = styled("form", {
  display: "flex",
  width: "100%",
});
